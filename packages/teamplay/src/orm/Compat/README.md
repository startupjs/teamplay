# Teamplay Compat Layer

This folder contains the remaining compatibility layer over base Teamplay
signals.

Current gap inventory:

- `INVENTORY.md` - factual compat / non-compat gaps.
- `MISALIGNMENTS.md` - decision matrix and cleanup order.
- `BEHAVIOR_MATRIX.md` - behavior table for current gaps only.

Current internal compat branches:

- default signal class selection through `Signal.ts` / `SignalCompat`.
